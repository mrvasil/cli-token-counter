services:
  token-counter-web:
    build: .
    container_name: token-counter-web
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "4173:4173"
    extra_hosts:
      - "host.docker.internal:host-gateway"
